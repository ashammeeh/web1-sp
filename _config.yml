
# Site settings
title: :BrAiN DuMp
description: A collection of logs/entries, responses to articles, photos, and exerpts of academic papers throughout the semester. 
baseurl: "/web1-sp" # folder where your site lives, e.g. /web1-sp - needs to start with a slash
url: " https://ashammeeh.github.io/web1-sp" # the base hostname & protocol for your site, e.g. http://hostname.domain.com

# Build settings
markdown: kramdown

defaults:  
  # template for pages unless an alternative is specified
  - scope:
      path: ""
    values:
      layout: "default"
